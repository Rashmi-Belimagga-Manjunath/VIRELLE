name: Keep Render awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: none

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping VIRELLE backend
        run: curl -sf -o /dev/null -m 60 https://virelle-live.onrender.com/api/health
